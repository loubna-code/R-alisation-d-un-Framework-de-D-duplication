package DAO.EXEL;

import DAO.CommonDataSource;

import javax.xml.transform.Result;

// Classe spécifique à EXEL
public class EXELDataSource implements CommonDataSource {
    // Implémentation spécifique à EXEL
    @Override
    public Result executeQuery(String query) {
        // Logique d'exécution de la requête pour EXEL
        return null;
    }
}
